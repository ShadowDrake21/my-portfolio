import { ThemeClassPipe } from './theme-class.pipe';

describe('ThemeClassPipe', () => {
  it('create an instance', () => {
    const pipe = new ThemeClassPipe();
    expect(pipe).toBeTruthy();
  });
});
